# Optional: Unsloth for faster Mistral 4-bit training only.
# Ignored when configs/mistral_qlora.yaml uses load_in_8bit: true (default for 12GB quality).
#
#   pip install -r requirements-optional.txt

unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
